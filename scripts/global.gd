extends Node

signal enemy_shooted(target)
signal player_shooted(target)

var map:Array


#(27,13) (28,13) (29,13)

#var ponto = map.pick_random()
#var pontoDireita = ponto + Vector2(1, 0)
#var pontoBaixo = ponto + Vector2(0, 1)
#
#if map.has(pontoEsquerda) and map.has(pontoDireita) :
	#var a = "coloco o sofá"
	
	
